export {};
// const API_URL = 'ws://localhost:9000/.netlify/functions/server';
// const socket = new EventSource(API_URL);

// socket.addEventListener('message', (event) => {
//   const message = JSON.parse(event.data);
// });
